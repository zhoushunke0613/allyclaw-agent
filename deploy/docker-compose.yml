version: '3.9'

services:
  allyclaw-agent:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    env_file:
      - ../.env
    restart: unless-stopped
    environment:
      - NODE_ENV=production
